# Jurimetria Preditiva em Acórdãos do TCU — dependências
# Trave as versões EXATAS após o primeiro ambiente funcionar: pip freeze > requirements.txt

# --- Dados ---
pandas>=2.2
pyarrow>=15.0
numpy>=1.26
tqdm>=4.66

# --- Modelagem clássica (baseline) ---
scikit-learn>=1.4

# --- Deep Learning / Transformers ---
torch>=2.2
transformers>=4.40
datasets>=2.19
accelerate>=0.30
peft>=0.10          # LoRA / QLoRA — Parameter-Efficient Fine-Tuning (Aula 08)

# --- PLN ---
nltk>=3.8
# Após instalar: python -c "import nltk; nltk.download('stopwords')"

# --- Aquisição de dados ---
requests>=2.31
pdfplumber>=0.11    # extração de texto de PDFs (campo voto, se necessário)

# --- Explicabilidade (diferencial) ---
lime>=0.2

# --- Visualização ---
matplotlib>=3.8
seaborn>=0.13
wordcloud>=1.9

# --- Qualidade de código ---
black>=24.0
ruff>=0.4

# --- Notebook ---
jupyter>=1.0
ipykernel>=6.29
